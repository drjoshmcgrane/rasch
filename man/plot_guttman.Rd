% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guttman.R
\name{plot_guttman}
\alias{plot_guttman}
\title{Plot the Guttman scalogram}
\usage{
plot_guttman(fit, max_persons = 80)
}
\arguments{
\item{fit}{A fitted object from \code{\link{rasch}}.}

\item{max_persons}{Persons are thinned to at most this many evenly spaced
rows for legibility on large samples.}
}
\value{
Called for its plotting side effect; invisibly \code{NULL}.
}
\description{
Displays the Guttman-ordered response matrix as a heatmap (dark for high
categories), with persons sorted by location down the rows and items by
location across the columns. The coefficient of reproducibility is shown in
the subtitle.
}
\examples{
set.seed(1)
d <- seq(-2, 2, length.out = 6)
X <- matrix(rbinom(200 * 6, 1, plogis(outer(rnorm(200), d, "-"))), 200, 6)
colnames(X) <- paste0("I", 1:6)
plot_guttman(rasch(X))
}
