% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_item_map}
\alias{plot_item_map}
\title{Plot the item map (location against fit residual)}
\usage{
plot_item_map(fit, band = 2.5)
}
\arguments{
\item{fit}{A fitted object from \code{\link{rasch}}.}

\item{band}{Fit residual acceptance band.}
}
\value{
Called for its plotting side effect; invisibly \code{NULL}.
}
\description{
Items plotted by location and fit residual, with the conventional
acceptance band at +/- 2.5 and misfitting items labelled.
}
\examples{
set.seed(1)
d <- seq(-2, 2, length.out = 6)
X <- matrix(rbinom(400 * 6, 1, plogis(outer(rnorm(400), d, "-"))), 400, 6)
colnames(X) <- paste0("I", 1:6)
plot_item_map(rasch(X))
}
